apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: notification-service-green
  region: us-east-1
  labels:
    app: notification-service
    version: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
      version: green
  template:
    metadata:
      labels:
        app: patient-service
        version: green
    spec:
      containers:
        - name: patient-service
          image: <your-green-version-image>
          ports:
            - containerPort: 3000
nodeGroups:
  - name: ng-1
    instanceType: t2.micro
    desiredCapacity: 3
